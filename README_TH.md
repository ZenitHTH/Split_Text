# YouTube Subtitle Manager & Splitter

เครื่องมือ Command-line ที่เขียนด้วย Rust สำหรับจัดการและแก้ไขซับไตเติ้ล YouTube ช่วยให้คุณสามารถดาวน์โหลดซับจากวิดีโอ YouTube, สแกนดูภาษาที่มีให้เลือก, และแบ่งไฟล์ซับขนาดใหญ่ (SRT) เป็นส่วนย่อยๆ เพื่อนำไปใช้งานต่อ

## 📜 ที่มา (Origin)

"เดิมทีโปรแกรมที่เขียนมาเพื่อแบ่งไฟล์ซับ( SRT ) ของไลฟ์ สตีมเมอร์ท่านหนึ่งที่ไลฟ์ความยาวมากกว่า 4ชม. แล้วนำไฟล์ซับที่แบ่งไปเข้า Google Gemini ในการทำ Timestamp แต่ฉันเวลาในการเขียนไม่ค่อยมี ส่วนมากจะเป็นการสั่ง Google Gemini เขียนโค้ด แต่ฉันเขียน Rust-Lang เป็นระดับหนึ่ง เลยจะเป็นการสั่ง LLM แล้วตรวจโค้ดซะมากกว่าการเขียนจริงๆ"

## ✨ ฟีเจอร์ (Features)

- **GUI Mode**: ใช้งานผ่านหน้าจอกราฟิก (Graphical User Interface) ที่สวยงาม พร้อมฟีเจอร์แสดงรูปปกวิดีโอ (Thumbnail) จาก YouTube
- **Scan Subtitles**: แสดงรายการภาษาซับไตเติ้ลทั้งหมดที่มีให้ดาวน์โหลดสำหรับวิดีโอ YouTube นั้นๆ
- **Download Subtitles**: ดาวน์โหลดซับไตเติ้ล (ในรูปแบบไฟล์ SRT) จากวิดีโอ YouTube
- **Split File (Auto)**: แบ่งไฟล์ขนาดใหญ่เป็นไฟล์ย่อยๆ อัตโนมัติ ตามจำนวนบรรทัดที่กำหนด
- **Split File (Manual)**: แบ่งไฟล์เป็นส่วนๆ ตามช่วงบรรทัดที่กำหนดเอง

## 🚀 การใช้งาน (Usage)

รันโปรแกรมโดยใช้คำสั่ง `cargo run` ตามด้วยคำสั่งย่อยและอาร์กิวเมนต์ที่ต้องการ

### 0. ใช้งานผ่าน GUI (Launch GUI)
รันคำสั่งเปล่าๆ เพื่อเปิดหน้าต่างโปรแกรม
```bash
cargo run
```
ในเวอร์ชันนี้ GUI สามารถดึงรูปปกวิดีโอ (Thumbnail) มาแสดงผลได้แล้ว!

### 1. สแกนหาซับไตเติ้ล (Scan)
ตรวจสอบว่าวิดีโอนี้มีซับภาษาอะไรให้ดาวน์โหลดบ้าง
```bash
cargo run -- scan <video_id_or_url>
```

### 2. ดาวน์โหลดซับไตเติ้ล (Download)
ดาวน์โหลดไฟล์ซับไตเติ้ล ค่าเริ่มต้นจะเป็นภาษาอังกฤษ (`en`) ถ้าต้องการภาษาอื่นให้ระบุต่อท้าย
```bash
cargo run -- download <video_id_or_url> [lang]
```
ตัวอย่าง:
```bash
cargo run -- download dQw4w9WgXcQ en
```

### 3. แบ่งไฟล์แบบอัตโนมัติ (Split Nth / Auto)
แบ่งไฟล์เป็นส่วนๆ ตามขนาดที่กำหนด (จำนวนบรรทัดต่อไฟล์)
```bash
cargo run -- nth <file_path> <lines_per_chunk>
```
ตัวอย่าง (แบ่งทีละ 1000 บรรทัด):
```bash
cargo run -- nth subtitles.srt 1000
```

### 4. แบ่งไฟล์แบบกำหนดเอง (Split Manual)
แบ่งไฟล์โดยระบุช่วงบรรทัดที่ต้องการ
```bash
cargo run -- manual <file_path> <range1> <range2> ...
```
ตัวอย่าง (แยกช่วงบรรทัดที่ 1-100 และ 200-300):
```bash
cargo run -- manual subtitles.srt 1-100 200-300
```

## 🛠️ การ Build

หากต้องการ Build โปรเจกต์เพื่อนำไฟล์ binary ไปใช้งาน (Release mode):

```bash
cargo build --release
```

ไฟล์ binary ที่ได้จะอยู่ที่โฟลเดอร์ `target/release/`
